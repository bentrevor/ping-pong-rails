<%= render "single_match", :match => @in_progress,
                           :winning_team => @winning_team %>

<%= link_to "Update scores", edit_match_path(@in_progress), {:class => 'button_link'} %>
<%= link_to "Finish match", matches_finish_path(@in_progress), {:class => 'button_link', :method => :post} %>

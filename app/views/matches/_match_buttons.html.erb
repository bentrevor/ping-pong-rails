
<%= link_to "Show match", match_path(match), {:class => 'button_link'} %>
<%= link_to "Update scores", edit_match_path(match), { :class => 'button_link' } %>
<%= link_to "Delete match", match_path(match), {:class => 'button_link', :method => :delete} %>
